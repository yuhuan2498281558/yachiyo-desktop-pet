# 项目协作说明

## 项目记忆路由

- 涉及项目目标、功能范围、验收标准、约束或工具偏好时，读取 `Prompt.md`。
- 涉及任务规划、继续工作、进度或下一步时，读取 `Plan.md`。
- 涉及代码修改、架构、模块边界、数据流或实现决策时，先读取 `Implement.md`，再检查相关源码。
- 涉及环境配置、依赖、运行、测试、构建或排障时，读取 `Documentation.md`。
- 同时涉及多个方面时，只读取对应文件，不必每次加载全部项目记忆。
- 完成任务后，只把本次确认且仍然有效的信息更新到对应文件；替换过时状态，不追加重复流水账。

## 事实来源与目录边界

- 源码、`package.json`、测试及 README 是最终事实来源；记忆与代码冲突时先核实代码，再更新记忆。
- `README.md` 是功能与操作说明的权威入口，素材授权以 `ASSET_LICENSE.md` 为准。
- `node_modules/`、`tmp/` 和 `release*/` 是依赖、临时或构建产物，不作为源码修改目标，也不得提交。

## 修改与验证要求

- 修改 JavaScript/CJS、配置或台词逻辑后，至少运行 `npm test`。
- 修改主进程、preload 或 renderer 时，除测试外还应执行相关文件的 `node --check`，并按风险手工验证启动、托盘、拖动、形态切换和退出。
- 修改打包配置时，按目标运行 `npm run build` 或 `npm run dist`，核对产物路径；日常运行不得把 portable 包当作已安装程序。
- 保持 Electron 安全基线：`contextIsolation: true`、`nodeIntegration: false`、`sandbox: true`，preload 只暴露必要 IPC。
- Codex 状态识别只用于本地生命周期判断；不得上传会话正文，也不得把正文自动送入模型服务。

## Windows 协作规则

- 默认使用 PowerShell 7；路径使用引号和 `-LiteralPath`，不要混用 PowerShell、CMD 与 Git Bash 的文件操作语法。
- 删除、移动或覆盖前解析并核对绝对路径；不对磁盘根目录、用户主目录或未解析变量做递归操作。
- 用户无法查看 Canvas 产物；需要可视化时使用普通文本、可点击本地 PNG 或可正常显示的可视化工具。

## 维护约束

- 先以代码、配置和权威文档为准，再更新项目记忆。
- 不记录密钥、令牌、密码、个人数据或 `.env` 的具体值。
- 无法确认的信息标记为“待确认”，不得写成事实。
- 保留用户已有内容和改动，不覆盖与当前任务无关的部分。
- 五个记忆文件保持 UTF-8，单文件尽量不超过 200 行。
