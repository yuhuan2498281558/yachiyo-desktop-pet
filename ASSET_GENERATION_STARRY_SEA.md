# “星降之海”场景素材记录

## 原片定位

- 原片：用户提供的 `超时空辉夜姬4K.mp4`。
- 演唱会段落：约 `27:40–30:30`。
- 鸟居、鱼群与下阶梯动作：约 `29:00–29:09`。
- 最终背景：用户提供的 1672×941 星空鸟居、樱花水面和水晶阶梯插画，转换为高质量 WebP；原片镜头只用于校准八千代动作与服装。
- 动作依据：原片 `29:04.701–29:09.373` 的逐帧复核；稳定触阶帧为 F69、F93、F119、F145，后 3 次间隔均为 26 帧（约 1.084 秒）。

## 生成方式

- 模式：Codex 内置 imagegen。
- 每个姿态单独生成，统一为 1320×1320 画布；没有把多帧挤在一张低分辨率精灵表中。
- 中间背景：纯 `#FFFF00`。
- 去底：优先使用 imagegen 技能提供的 `remove_chroma_key.py`；对 5 张红伞会被通用去色误伤的过渡帧，改用只移除与画布边缘连通黄底的遮罩，保留肤色、红伞和人物内部高光。
- 运行时格式：透明无损 WebP。
- `00–11` 是一个连续低抬腿步态周期：并脚支撑、抬脚、前摆、下探、前掌接触、压平、重心转移、后脚收跟。运行时把同一周期连续用于四次换阶，不插入停顿或反放。

## 鱼群叠加素材

- 近景层：大鲸、幼鲸、鳐和飞鱼共 4 体，带蓝青紫晶格、星点和长粒子尾迹。
- 远景层：10 条小型鲸/鳐/飞鱼组成宽幅 S 形鱼流。
- 两层均由内置 imagegen 以用户背景作为风格参考生成，再通过纯 `#00FF00` 色键和 `remove_chroma_key.py` 转为透明无损 WebP。
- 动画中使用不同速度、方向、景深和透明度，避免遮挡鸟居、角色及水晶阶梯。

## 最终提示词模板

> 以原片 29:04.701–29:09.373 的脚部特写和全身逐帧联系表为最高优先级，生成一张八千代在“星降之海”演唱会中沿发光阶梯向屏幕左下行走的高清全身 Q 版动画帧。保持原片服装和身份：白色超长发、双环发髻与青绿色发饰、青绿色眼睛、深海军蓝和服式外衣、胸前粉色玩偶、青绿色内裙、浅青长袜与星点、完整包头白色厚底浅口鞋。她在胸前稳定握住展开的深朱红日式和伞；伞杆向画面右上倾斜约 45–50°；伞面采用原片侧向窄楔形透视，禁止画成正面圆盘，禁止水平镜像。步态必须低抬腿、近乎平掌落地，头肩和伞保持稳定；长发滞后 4–6 帧，裙摆滞后 2–4 帧。完整显示人物、鞋、全部发尾、伞柄、伞尖和完整伞面，保留足够边距。纯 `#FFFF00` 背景；无鸟居、无鱼、无阶梯、无阴影、无文字、无边框、无额外人物、不是精灵表。

逐帧只改变腿部和重心：

- `00–02`：并脚支撑到脚跟轻抬。
- `03–05`：前脚低位前摆、越过支撑脚并向下一阶下探。
- `06–08`：鞋底逐渐找平，前掌接近接触。
- `09–11`：前脚触阶、压平承重，后脚抬起并开始收跟。

## QA

- 十二张运行时帧均为 1320×1320 RGBA，四角完全透明。
- 可见像素平均 alpha 为 241.8–246.0，近乎不透明像素占 91.5%–94.4%；红伞、肤色与鞋面不会在换帧时发灰。
- PNG 与无损 WebP 的 alpha 和可见像素逐像素一致。
- 运行时总长 4.8 秒：0.15 秒准备，4×1.05 秒连续换阶，0.45 秒收势；没有高抬膝、跳阶、深蹲回弹或额外第五次跨步。
