# 桌宠优化验收与后续建议

## v0.3.3：画质、视线和诊断（2026-09-06）

- 标准/省电分别最高 60/30 FPS、最高 2×/1× 渲染，实际不超过屏幕比例。标准保持默认；不重载模型，不修改两张 4096 纹理，不能据此声称纹理内存下降。
- 隐藏后停止模型时钟；显示、尺寸和画质切换先绘制首帧。Live2D 自动散步仍被禁止，拖动仍是单张快照平移。
- 视线按时间缓动，增加柔和转头/仅眼睛选项。点击轮换 smile/shy/bsmile，有 900 ms 冷却；双击允许升级为 hop，重复 hop 不重启。表情异步失败已捕获，拖动中延期恢复中性表情。
- 托盘新增“关于与诊断”：版本、模型状态、程序与日志目录；渲染进程退出有独立故障提示。无新增依赖或 preload 权限。
- 工程审查覆盖主进程、偏好、renderer 调用链及测试；独立审查指出的双击冷却冲突和故障状态遗漏均已修复。未重写状态机或修改模型源文件。

验证：27 项单元测试、语法检查、源码及打包版各 11 组真实 Electron 回归通过。两档画质下实际输入拖动均有非透明且不变的快照，停止模型时钟，松手恢复；视线/画质偏好退出后保存，正常重启仍显示 Live2D。

- 源码报告：`tmp/electron-smoke-1788704417970/report.json`。
- 打包报告：`tmp/electron-smoke-1788704505876/report.json`；本机 150% DPI、各预热 1.2 秒后采样 6 秒：标准 57.56 FPS、516×560 渲染、CPU 7.38%；省电 29.50 FPS、344×373 渲染、CPU 5.46%。这是短时进程 CPU 汇总，不是续航测量或跨设备承诺。
- 已查看重启截图，模型完整显示；省电在高 DPI 下较柔和。本轮未重复上轮的半小时长测，也未逐帧录制 Windows 桌面合成输出，不保证所有设备无闪烁。
- 已部署 `D:\桌宠\程序\八千代与辉夜桌宠\八千代与辉夜桌宠.exe`；构建 `tmp/comfort-build-20260906/win-unpacked`；安装 app.asar SHA256：`B9ADDCBCA069DB5F5D14D22A423C661C5EC12049814B7C6B28B9ED649DAF01F5`。
- 回滚备份：`D:\桌宠\程序\八千代与辉夜桌宠-backup-20260906-pre-0.3.3`，包含旧程序及升级前偏好。源码快照：`tmp/before-comfort-20260906`。未提交或推送 Git。

下一步建议：先处理下文的“超长任务状态边界”，再补发布前模型/运行库哈希清单。2048 纹理与演唱会按需加载仍待评估；本轮不把降低渲染分辨率当作贴图压缩已完成。

## v0.3.2 历史验收

日期：2026-09-06。范围：本地个人桌宠的稳定性、拖动、状态监测与可验证交付。

## 本轮落地

- 偏好存储：字段校验、同目录临时文件/fsync/原子替换、退出 flush。替换失败保留旧配置，错误不再静默消失。
- 状态监测：同步扫描移入 worker，缓存文件大小/修改时间及分类结果；只向主线程传递聚合状态，worker 异常记录错误码并延后重启。
- 行为协调：主进程持续持有拖动状态，renderer 延后处理工作/偏好/场景事件；已开始的 Sprite 变身也在拖动期间暂停。重复工作通知和任务数量变化不再打断变身。
- Live2D：双端禁止自动散步，即使已有偏好为 true；手动拖动只使用一次静态快照。松手先绘制实时帧再撤快照，取消窗口拖动时的透明渐变。
- 统一时钟：关闭模型默认 shared ticker，由应用 ticker 驱动模型，拖动时模型时间也停止；恢复帧最多推进 50 ms，最高 60 FPS，避免长时间暂停后物理动画跳变。
- 生命周期：异步加载完成前退出能销毁迟到的模型；失败后不因重复偏好广播无限重试，切换 Sprite 再切回可重试并保留 DOM canvas；WebGL 上下文丢失可安全回退。
- 诊断：仅记录状态/错误码，两个日志文件各最多 256 KiB，不记录会话正文、会话路径和原始错误文本。

## 验证证据

- `npm test`：24 项通过，涵盖偏好异常与原子替换失败、junction 目录、缓存更新/删除、worker 生命周期、散步互斥、拖动延期、重复状态、模型加载与时钟暂停。
- `npm run check`：应用和验证脚本语法通过，排除第三方 vendor。
- `npm run test:electron`：源码版本 9 组真实 Electron 操作/重启检查通过。独立偏好目录，不改日常配置。
- `npm run benchmark:activity`：本机 22 个近期会话首次扫描约 104 ms；缓存后约 4–6 ms，未变化文件读取数为 0；文件变化时仅重读变化文件。首次扫描已在独立线程运行。
- 渲染长测与重启通过：1800.01 秒、60 次采样、10 组检查；报告：`tmp/electron-smoke-1788701448913/report.json`，`passed: true`。窗口未自行位移，无 Live2D 回退；重启后 `live2dActive: true`、`walking: false`。
- 长测工作集汇总读数范围 673–776 MiB；CPU 汇总读数均值约 7.70%（排除首次无基线的 0）。后续仍值得优化渲染资源占用，不能把最初约 5% 的样本当作全程表现。
- 部署时发现日常数据目录的 C→D junction 下创建日志子目录失败，补充真实路径解析。最终打包版为 `tmp/reliability-build-release-20260906/win-unpacked`；新增 junction 单元测试、最终包 9 组真实 Electron 回归与重启均通过，报告：`tmp/electron-smoke-1788703592173/report.json`。
- 最后补丁仅影响存储路径解析；最终包的 renderer、行为协调和 worker 与半小时长测包逐字节一致，因此未重复运行整段渲染长测。

验证边界：测试检查桌宠自身的画布、真实输入和窗口坐标；并未逐帧录制 Windows 桌面合成输出，所以不能据此保证所有显卡/DPI 组合均无视觉闪烁。资源样本为本机测量，工作集相加可能重复计算共享页面。

## v0.3.2 时提出的优化候选（前三项现已按上述范围落地）

1. **省电画质档**。当前模型有两张 4096×4096 纹理，RGBA 原始像素量合计 128 MiB（不等于进程实占显存）。建议提供“标准/省电”档，分别比较 60/30 FPS、4096/2048 贴图，在三档尺寸验证发丝和脸部清晰度。按需加载演唱会帧、隐藏时明确暂停模型，也应一起评估。先测 CPU、GPU 进程内存及可见质量，再决定默认值；不能只缩小文件大小就宣布内存下降。
2. **视线缓动与表情互动**。全局视线目前每 50 ms 推送，模型参数直接赋值；这比真正的自然头部转向更容易显得生硬。建议 renderer 保存目标视线，每帧按时间缓动，并开放“只动眼睛/轻微转头”的选项。模型已有 14 个表情配置，当前点击主要使用 smile/surprised，可增加带冷却时间的表情轮换。仍需用真实模型验证遮挡和侧头上限。
3. **版本与模型状态入口**。增加简洁的“关于/打开程序目录/当前形象状态/打开诊断日志”入口，帮助确认正在运行哪一版，以及是否已经回退到 Sprite。优先补托盘入口，暂不需要完整设置窗口。
4. **超长任务状态边界**。扫描器当前使用 8 小时文件修改时间窗口；若某工作任务超过该时长且没有新事件，可能被排除。应为已确认活跃的任务保留跟踪，并仅以终止事件结束；同时设计强制中断后的人工恢复方式。该项是根据源码推导的边界风险，本轮未做 8 小时实测。
5. **素材与发布的可复现性**。模型目录不进 Git，CI 不能覆盖真实 Live2D。可在本机发布前固定运行 packaged smoke，并记录模型/运行库哈希与版本；有素材授权前提后再考虑受控 CI 素材配置。

本轮不需要完整重写 renderer，也不需要引入外部动画框架。真实跑步/侧头质量仍受现有模型源文件限制；应取得可编辑 Cubism 工程或真实分层美术后再制作动作，当前保留固定待机体验。

## 回滚与资料

- 修改前源码/测试快照：`tmp/before-reliability-20260906`，保留了本轮开始时尚未提交的 Live2D 集成。
- 旧日常程序及原偏好：`D:\桌宠\程序\八千代与辉夜桌宠-backup-20260906-pre-0.3.2`；备份 app.asar 与原程序 SHA256 一致。
- v0.3.2 已部署并启动：`D:\桌宠\程序\八千代与辉夜桌宠\八千代与辉夜桌宠.exe`。保留原偏好；日常日志已确认 startup 0.3.2 与 Live2D ready。
- 安装目录 app.asar 与最终构建 SHA256 一致：`5D4EA0EB741AAD8AA3720A5024703C91F1F20BBA457786FAB329C4DC3AC4EE09`。
- 本轮未提交或推送 Git。依赖版本未变，只把应用版本及锁文件根版本同步为 0.3.2。
