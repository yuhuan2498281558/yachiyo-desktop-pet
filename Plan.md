# 计划与进度

最后更新：2026-09-07

## 当前轮：v0.3.9 收小步幅

- 用户认为 v004 跨步太大、不优雅。内置 imagegen 定向编辑为小步轻跑，低抬脚、直立侧身、收敛衣发摆动；透明处理及统一眼锚点，不改变 280 ms 步频和拖动逻辑。
- 新素材 `yachiyo-hd-run-gentle-v5.webp` 已接入，v004 宽步图保留历史但不打包。40 项单元测试、语法检查和最终打包版 18 组实际回归通过，左右截图已目视检查；报告 `tmp/electron-smoke-1788715139342/report.json`。已保留偏好部署并启动，旧版备份后缀 `pre-0.3.9`。原稿/提示词/预览在 `D:\桌宠\资料\Sprite高清重绘\八千代小步轻跑-v005`。未推送 Git。

## 上轮：v0.3.8 官方侧视参考奔跑重绘

- 用户明确“原地跑”指造型仍然面朝正面，并补充官方设定集。已从指定 PDF 第 21 页（书内 19 页）核对侧脸和全身侧视，内置绘图生成四帧完整右侧视，左向镜像真实侧视帧。
- 新素材 `yachiyo-hd-run-side-v4.webp` 已完成透明/发束间隙处理、统一尺度和头部对齐，旧正面跑步图不再使用或打包。保留 280 ms/帧、拖动方向触发、停住/松手待机及原视线跟随。
- 40 项单元测试、语法检查及最终打包版 18 组实际窗口检查通过，已目视核对左右侧身和肩颈无拼接缝；最终报告 `tmp/electron-smoke-1788714303799/report.json`。已备份并更新日常程序，保留最新偏好；旧版在 `D:\桌宠\程序\八千代与辉夜桌宠-backup-20260907-pre-0.3.8`，未提交或推送 Git。原稿、参考、提示词和预览在 `D:\桌宠\资料\Sprite高清重绘\八千代侧身奔跑-v004`。

## 上轮：v0.3.7 拖动方向奔跑与减速

- 按用户纠正移除原地跑步按钮，改为真实左右拖动触发对应方向奔跑；280 ms/帧，停住或松手回待机，8 px 反向过滤，窗口保持跟手。不改动原画素材，Live2D 仍冻结平移。
- 39 项单元测试及语法检查通过；源码与最终打包版各 18 组真实窗口检查通过，含左右拖动/停住/松手和旧原地命令失效，最终报告 `tmp/electron-smoke-1788713133262/report.json`。
- 已部署并启动日常程序，保留已保存偏好；旧版及偏好备份 `D:\桌宠\程序\八千代与辉夜桌宠-backup-20260907-pre-0.3.7`。最终构建 `tmp/hd-drag-run-final-20260907/win-unpacked`，app.asar SHA256 `4D5995BE4D04F460E3C68A717DF73397C798A017E483618B8F542A847C8C4314`。未提交或推送 Git。

## 上轮：v0.3.6 奔跑动作与鼠标视线

- 已按 v003 绘制四帧交替抬腿动作并做透明处理、同尺度对齐和头部锁定；新增“原地跑步（高清）”，手动播放约 2.1 秒后回待机。继续禁止高清自动散步，拖动不触发跑步。
- 已基于原待机素材生成眼部方向图块，加入全局鼠标视线缓动、关闭复位和眨眼/奔跑遮挡。未改变已确认待机母图，也未扩展辉夜/变身/演唱会动作。
- 39 项单元测试和语法检查通过；源码及最终打包版各 18 组 Electron 检查通过，报告分别为 `tmp/electron-smoke-1788712182853/report.json` 与 `tmp/electron-smoke-1788712402465/report.json`。运行素材与哈希在 `assets/sprites/yachiyo-hd-motion-v3.json`，本地原稿、预览和提示词在 `D:\桌宠\资料\Sprite高清重绘\八千代动作-v003`。
- 已部署并启动 v0.3.6，保留用户最新全部偏好（此时已显示桌宠），未启用高清自动散步。旧版及偏好备份在 `D:\桌宠\程序\八千代与辉夜桌宠-backup-20260907-pre-0.3.6`；最终构建 `tmp/hd-motion-final-20260907/win-unpacked`，app.asar SHA256 为 `0F34E8A3591330C52A80CCF6D516F4B978CCCA9A74D5F0F0F09B5D70E66C0DA5`。启动日志 `tmp/hd-motion-launch-20260907`。未提交或推送 Git。

## 上轮：v0.3.5 按已确认形象重构

- 用户认可 v003 并要求按该形象重构。已完成原母图透明处理、眼部帧合成及 `yachiyo-hd-idle-v3.webp` 接入；v1 不再引用/打包，经典 Sprite 与 Live2D 保留。
- 新素材每帧 768×832，母图等比显示为 429×780，长袍与发型不拉伸；眨眼仅改变 `(347,114)-(424,142)` 内像素，所有帧透明轮廓一致。发环、颈侧及长发间隙透明，浅色背景残边已做一像素清理与抗锯齿。
- 33 项单元测试及语法检查通过，首轮源码与最终打包版各 15 组 Electron 通过；最终报告 `tmp/electron-smoke-1788710918204/report.json`。顺手修正测试脚本把合法的 -0 角度误判为非零的问题，不改变 Live2D 行为。
- 最终构建：`tmp/approved-sprite-final-20260907/win-unpacked`；菜单名“高清 Sprite（新版八千代）”。未提交或推送 Git。
- 已部署并启动于日常程序目录，启动日志确认 v0.3.5，安装包 app.asar SHA256 为 `2F2218CF57A6D6B7420F0110EDB28933504ABB9181A5BC79BDC04C5207BABE5E`。旧程序及偏好备份在 `D:\桌宠\程序\八千代与辉夜桌宠-backup-20260907-pre-0.3.5`；只将形象选为 `sprite-hd`，保留原隐藏状态、位置、大小及其他偏好，从托盘“显示桌宠”查看。启动日志在 `tmp/approved-sprite-launch-20260907`。

## 上轮：v0.3.4 高清 Sprite 待机预览

- 已按设定集校准八千代脸、发环、服装与玩偶，完成睁眼/半闭眼/闭眼稿；内置绘图生成，本地脚本处理真 Alpha、去绿和眼部合成，运行单帧 768×832。
- 已新增右键/托盘“高清 Sprite（待机预览）”，保留经典/Live2D。高清仅待机眨眼，拖动冻结平移、不自动散步、不因连续点击触发旧动作；辉夜/变身/演唱会保留原素材。
- 32 项单元测试、语法检查及源码/首轮打包版各 14 组 Electron 回归通过；最终去绿素材的打包版 15 组通过，新增高清偏好进程重启验证。报告：`tmp/electron-smoke-1788708329724/report.json`。
- v0.3.4 已部署并启动于原日常程序目录，保留原 Live2D 偏好；旧版及偏好备份在 `D:\桌宠\程序\八千代与辉夜桌宠-backup-20260907-pre-0.3.4`。最终构建 `tmp/hd-sprite-final-20260907/win-unpacked`，app.asar SHA256：`01BC83A30C5ADD70C93E1FC9AA34A357B56A00E076E91D01668665DCB634E08E`。
- 新稿与透明处理在 `D:\桌宠\资料\Sprite高清重绘\八千代待机-v001`，实现与提示词见 `HD-SPRITE.md`。本轮未提交或推送 Git；书页与中间稿不上传。
- 用户反馈 v001“不够还原”，美术验收未通过；不能把运行/透明测试通过当成造型已认可。暂停扩展动画与辉夜组。
- 已重新核对设定集第 20–21 页与画集：v001 圆眼/刘海、交叉领、短蓬裙、缺失金色腰封及纹样偏差明显。内置绘图制作 v002a/v002b 静态候选，纠正露肩、眼型、长后摆、腰封、带点菱纹和长发；仍待用户认可，尚未抠图、制作帧或部署。候选与提示词在 `D:\桌宠\资料\Sprite高清重绘\八千代待机-v002`，原运行素材未覆盖。
- 用户认可 v002b 方向后要求只调整发型，v003 调整刘海、发环根部、短发簪和两侧束发，并去除垂至胸前的细长鬓发。局部蒙版保留已认可底稿，眼睛、嘴部及蒙版外像素一致；用户随后认可 v003 并要求接入（见本轮）。

## 仓库公开与模型范围

- v0.3.3 累计代码已以 `1a42631` 推送；仓库按用户要求改为 Public。
- 用户确认当前八千代 Live2D 运行包可在注明非商业用途后公开提供；本次将该目录纳入 Git，保留来源、权利归属及非商业说明。其他模型、原画集、设定集、PSD、Cubism 源工程和本地用户数据不上传。

## 当前轮：v0.3.3 体验优化

- 已完成标准/省电画质（60/30 FPS、最高 2×/1× 渲染），即时切换并保留模型；隐藏时明确暂停。
- 已完成时间缓动视线、柔和转头/仅眼睛选项、带冷却的友好表情轮换，保留双击跳跃与拖动冻结。
- 已完成托盘版本、模型状态、程序目录和日志目录入口；不增加 renderer 文件访问权限。
- 27 项单元测试、语法检查、源码与打包版各 11 组 Electron 回归通过。v0.3.3 已部署并启动，旧版及偏好保留在 `D:\桌宠\程序\八千代与辉夜桌宠-backup-20260906-pre-0.3.3`。
- 下一步候选：超长任务状态跟踪边界、发布前模型/运行库哈希清单；本轮不继续扩大范围。详情见 `Optimization-report.md`。

## 上轮长任务：v0.3.2（已完成）

1. 偏好：字段校验、原子替换、退出前刷新；验证立即退出与损坏配置恢复。
2. 状态扫描：移入独立 worker，缓存未变化文件；验证事件准确性、扫描成本和主线程响应。
3. 行为协调：拖动优先、阻止过期散步和重复变身；Live2D 固定待机，只允许手动平移。
4. 诊断：有大小上限的本地结构化日志，仅记录状态和错误码。
5. 回归与交付：测试、语法检查、真实 Electron/解包验证、保留旧版并部署；复核下一批优化。

进度：全部完成。24 项测试、语法检查、真实 Electron 输入/重启回归、30 分钟渲染长测通过；v0.3.2 已部署到日常目录并启动。长测后补修用户数据 junction 的日志目录创建问题，新增 junction 测试和最终打包版 9 组回归通过；渲染/行为/worker 与长测版本逐字节一致。源码快照：`tmp/before-reliability-20260906`；详细证据与后续建议见 `Optimization-report.md`。

## 当前里程碑

- 完成 v0.3.2 可靠性升级及可回滚的 v0.3.3 画质、视线、表情和托盘诊断优化；保持 Live2D 固定待机，拖动仅平移。

## 已完成

- 完成八千代/辉夜双形态、Codex 生命周期联动、视线追随、托盘偏好、散步、使魔和“星降之海”场景。
- v0.3.4 当前单元测试 32/32 通过；Windows 解包版从 D 盘运行，快捷方式不再启动 portable 包。
- 用户数据已迁移到 D 盘，原应用数据入口通过目录联接保持兼容。
- 建立独立 Git 仓库；v0.3.2、Live2D 集成与 v0.3.3 已一并推送到 main。
- 初始化 `AGENTS.md`、`Prompt.md`、`Plan.md`、`Implement.md` 和 `Documentation.md` 项目记忆。
- 建立 GitHub Actions Windows CI：pull request 运行测试与语法检查，推送 `main` 或手动触发时增加 unpacked 最小打包检查。
- 将默认构建改为 `win-unpacked`，默认发行改为 NSIS；portable 只保留显式构建命令，避免误作日常启动入口。
- 在 `D:\桌宠\资料\Live2D工程\八千代` 建立本地 Live2D 工作区，完成八千代 v0.2 模型规格、原片/设定集/画集参考索引及侧头重绑参考包；素材与衍生图不进入 Git。
- 核实 GitHub 原版、贴图实验版和此前标记为“官方重制版”的 `.moc3` 哈希完全相同；当前没有真正重绑的新模型，预览页已改为如实标注。
- 完成阶段 0 事实核验并建立 `D:\桌宠\资料\Live2D工程\八千代\03-Cubism工程\04-真实侧头重绑_v1` 只增不改工作区；v006 仅作为运行时反推视觉检查点保留，未冒充真实分层绑定源。
- 生成后脑勺、束发汇聚、后颈发片和发环前后层的四视角结构草稿，作为真实分层美术重绘参考；尚未导入 Cubism。
- 根据第四张近侧视反馈修正扎发根部纹理和后脑收束，结构草稿 v002 为当前参考，仍未导入 Cubism。
- 建立真实分层图层清单 v001，明确后脑底发、束发、后颈、发环、发簪、近远侧发和淡紫内层发的目标层及遮挡关系。
- 生成分层目标骨架 PSD 检查点，新增 19 个带 `TODO` 的空层，不改变 v006 原有像素；下一步进入逐层重绘/补边。
- 按官方背面布局完成第一组后脑勺/束发合并草稿 v005：发圈与后脑侧发连续、辫束穿过发圈内侧/下缘、发簪改为短细错开形；已通过 RGBA Alpha 校验并放入底层检查点，仍未冒充真实分层源或导入 Cubism。
- 修正正面两个发圈叠加问题：建立 v007 正面匹配检查点，后层草稿默认隐藏；与原 v006 正面预览逐像素对比为零差异。后续侧头重绑必须以原版正面发圈比例作为锁定基准。
- 从 v006 PSD 提取原版发圈/发簪的精确 RGBA 基准并加入 v008 隐藏参考层；正面仍与原版逐像素一致，下一步按该基准拆分左右发圈前后层。
- 修正发圈整体缩放失衡：建立 v011，后脑/长辫草稿移除上部发圈和发簪区域，正面继续使用原版发圈，确保新后层不改变发圈大小比例。
- 将 v006 原版附件拆为 6 个独立隐藏 RGBA 参考层并建立 v012：左右发圈、左右上发簪、左右下发簪；正面预览与原 v006 逐像素一致。下一步以这些层为几何锁，人工拆分/重绘对应的前后层并处理侧头遮挡关系。
- 建立 v013 正面附件拆层：隐藏原 `ArtMesh421–426`，由 6 个同像素的 `Loop_*_Front__TARGET_v001` / `Hairpin_*__TARGET_v001` 按原 z 顺序接管显示；正面预览仍与 v006 逐像素一致。下一步开始制作左右发圈的后层连接和侧头遮挡。
- 建立 v014 正面预览清理基线：隐藏 `ArtMesh16` 的闭合面部圈线，隐藏 `ArtMesh251/302` 的丝带下方混合阴影色块；白色垂带前层保留并改为语义化目标名，左右阴影层转为 TODO，后续需按真实分层美术重绘。
- 建立 v015 脸部与裙摆清理基线：隐藏残留红色闭合轮廓 `ArtMesh220`，隐藏导致中央裙片周围灰色 U 形色块的 `ArtMesh273/276`，并将其标为待重绘左右裙片；正面恢复深紫中裙片、青绿色侧裙片和白色垂带的清晰遮挡关系。
- 建立 v018 正面细节基线：用低透明度柔和渐变替换 `ArtMesh2/14` 的圆形腮红蒙版，保留原图淡粉腮红但去掉脸颊闭合圈；裙摆维持原参考图的深紫中裙片、肉色腿部和青绿色侧裙片关系。
- 建立 v020 腿部修正检查点：恢复两侧大腿外缘的青色布料，仅保留中央肉色大腿与深紫裙片，并保留裙摆阴影。
- 建立 v036 脸部交叠清理检查点：当时误判白线来自侧发交叠；用户复核后问题仍在。
- 建立 v037 脸部与裙摆修正：隐藏扩边 `Face_Fill` 和 `FUSE2/FUSE3` 后面部变形，且丝带内侧大腿被青色盖住。
- 建立 v040 后出现全身噪点和嘴部白块；已用 v041 回退到 v018 干净底稿，弃用 v037–v040。
- 已将 GitHub 原版 `tsukimi-yachiyo` 作为本地可选八千代 Live2D 形象接入桌宠；待机、散步与拖动均保持 Live2D，保留 Sprite 作为资源缺失、WebGL 失败、变身和演唱会回退，并新增持久化右键/托盘形象切换。
- 完成 Pixi/Cubism 严格 CSP 适配、全局鼠标视线参数映射、点击表情、三档窗口自适应、自动散步的程序化脚步/身体跑动、6 px 拖动死区、32 ms 位移合并、手动拖动静态快照、单双击合并与真实 Electron 行走/拖动快照验收；测试基线更新为 12 项。

## 进行中

- Live2D 侧头结构重制准备：已建立后脑勺束发、后颈内收、发环遮挡和 `Angle X` 五姿态验收标准；现有开源运行时模型作为参数/物理基线。驱动隔离实验 v001 已验证 physics 衰减不能改变静态后脑轮廓，下一步必须取得真实分层美术或合法 Cubism 源工程。

## 待办

- 已完成：偏好模块化、类型校验、同进程同步串行原子写入和退出 flush；覆盖写失败与恢复测试。
- 已完成：本地限额诊断日志，覆盖生命周期、散步、拖动、模型状态、场景和配置/worker 错误。
- 已完成：轻量行为协调模块、拖动优先和工作状态去重；完整 Sprite 动作队列尚不需要引入。
- 待确认：增加基于版本 tag 的 NSIS 发布流程，并评估代码签名。
- 待确认：按本地规格先绘制中性全身定稿和正/侧/背结构补图，再制作头部、头发、上半身与粉色玩偶的最小可动样机。

## 阻塞

- 角色和作品相关素材目前仅适合个人学习与桌面展示；公开或商业发布受素材授权约束。
- 本机已安装 Live2D Cubism Editor 5.3.03，但没有可编辑的 `.cmo3` 源工程；现有 `.moc3` 无法反向修改 ArtMesh/Deformer，因此不能通过贴图或网页参数真正修复侧头后脑勺结构。
- 当前 `02-分层工程/八千代_半身MVP_分层工程.psd` 是部件位置占位稿，不是真实可绑定的角色美术。
- 原始设定集 PDF 第 21 页已重新核验，包含八千代服装正、侧、背造型，不再以“缺少三视图”为阻塞；另用画集第 13/29 页核对全身、面部和玩偶。旧 P22–P25 PNG 不作为权威依据，真正可编辑分层源仍缺失。

## 下一接手点

- 若继续 Live2D，先按 `D:\桌宠\资料\Live2D工程\八千代\02-分层工程\八千代_真实分层美术拆件Brief_v001.md` 取得可靠的结构设定和真实分层美术，再从真实分层美术新建模型或取得合法 `.cmo3` 源工程；侧头任务单位于 `D:\桌宠\资料\Live2D工程\八千代\03-Cubism工程\侧头修正\侧头重绑任务单.md`。可靠性首批改造已完成，下一批按验收报告中的收益排序选择。
